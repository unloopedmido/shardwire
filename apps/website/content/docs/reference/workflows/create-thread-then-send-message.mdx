---
title: "createThreadThenSendMessage"
description: "Create a thread from a parent channel (optionally on a message), then send a message in that thread."
---

`function` exported from `src/workflows/thread.ts`.

## Summary

Create a thread from a parent channel (optionally on a message), then send a message in that thread.

## Signature

```ts
/**
 * Create a thread from a parent channel (optionally on a message), then send a message in that thread.
 *
 * @see https://unloopedmido.github.io/shardwire/docs/reference/workflows/create-thread-then-send-message/
 */
export async function createThreadThenSendMessage(app: Pick<AppBridge, 'actions'>, args:;
```

## Related Guides

- [Workflow Helpers](/docs/guides/workflows)
